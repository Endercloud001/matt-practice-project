# Domain Docs

This is a single-context repo. Before exploring, read the root `CONTEXT.md` and any relevant decisions in `docs/adr/`.

Use the glossary vocabulary from `CONTEXT.md` in issue titles, refactor proposals, hypotheses, and test names. If a needed concept is missing, treat that as a signal for domain modeling.

If an output contradicts an existing ADR, surface the conflict explicitly rather than silently overriding it.

Expected layout:

```text
/
├── CONTEXT.md
├── docs/adr/
└── src/
```
